# Sessie 2026-08-28 (middag) — bug-squad: #77 + #51 parallel

## Werkwijze

Twee sonnet-subagents parallel in eigen git-worktrees (bug-squad-skill), elk
issue end-to-end: root-cause → fix → browser-smoke (desktop + 360px mobiel)
→ PR. Coördinator-pass daarna: twelve-rules-review over beide diffs,
merge-gate-check, cross-bug-onafhankelijkheid. Merge pas na groen licht.

## PR #79 — ui-scale-125 zoom-drift chat-popups + exportmenu (#77)

7 sites gefixt (msg-kebab ×2, ctx-popup ×2, ctx-detail-popup,
memory-used-detail in `chatRenderer.js`; exportmenu `app.js:433`) met nieuw
helpermodule `static/js/uiZoom.js` (`zoomOf`/`toLocalPx`, 6 node-tests in
`tests/test_ui_zoom_js.py`). Gotcha gedocumenteerd in de module: `zoomOf`
altijd op `document.documentElement` — een net-geappende popup leest stale
`currentCSSZoom` (=1) en dubbelt de schaling alsnog. `offsetHeight`/
`scrollHeight` zijn al local-space, niet delen. Smoke: 4 combo's
(100/125% × desktop/mobiel), delta's exact 0; kebab op mobiel-125% correct
tegen de rechterrand-clamp. Restsites in andere subsystemen (notes, docs,
email, compare, editor, …) → **issue #80**.

## PR #78 — ontbrekende mp4-renditions landing page (#51)

8 H.264-mp4's gegenereerd (ffmpeg, crf 28; `bg.mp4` crf 36 → 2.84 MiB,
kleiner dan de webm) + source-volgorde mp4-eerst. Totaal +5.6 MB repo.
Smoke: alle 8 `currentSrc=*.mp4`, `readyState=4`, 0 404's, beide viewports.

## Lessen

- **PR-description-gate**: subagent-PRs met vrije-vorm-body vallen op de
  template-check (`pr-description-check.yml` eist Summary/Linked Issue/Type/
  Checklist/How to Test). Body achteraf ombouwen kan via
  `gh api -X PATCH repos/.../pulls/N -F body=@file`; de check hertriggert op
  `edited`. Volgende keer: template in de agent-brief meegeven.
- Query-loze ES-imports (`./chatRenderer.js`) zijn geen stale-cache-risico:
  de app serveert `cache-control: no-cache` + ETag; de buster is extra.

## Status

Beide PRs squash-gemerged (`84ed4f5`, `fc4cb6f`), branches + worktrees
opgeruimd, prod herbouwd en geverifieerd (menuzoom2 + uiZoom.js live op
:7000). Ed checkt op prod nog één ctx-popup op 125% (TOTP; enige groep die
alleen synthetisch geverifieerd is). #51-fix op Pages volgt bij de
eerstvolgende main-promotie.

Ed de Feber, in nauwe samenwerking met Claude
